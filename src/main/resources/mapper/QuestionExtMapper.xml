<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gs.community.mapper.QuestionExtMapper">
    <resultMap id="BaseResultMap" type="com.gs.community.model.Question">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Mon Dec 16 10:41:47 CST 2019.
        -->
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="title" jdbcType="VARCHAR" property="title"/>
        <result column="gmt_create" jdbcType="BIGINT" property="gmtCreate"/>
        <result column="gmt_modified" jdbcType="BIGINT" property="gmtModified"/>
        <result column="creator" jdbcType="INTEGER" property="creator"/>
        <result column="comment_count" jdbcType="INTEGER" property="commentCount"/>
        <result column="view_count" jdbcType="INTEGER" property="viewCount"/>
        <result column="like_count" jdbcType="INTEGER" property="likeCount"/>
        <result column="tag" jdbcType="VARCHAR" property="tag"/>
    </resultMap>
    <update id="incView" parameterType="com.gs.community.model.Question">
        update question
        set view_count = view_count + #{viewCount,jdbcType=INTEGER}
        where id = #{id}
    </update>
    <update id="incComment" parameterType="com.gs.community.model.Question">
        update question
        set comment_count = comment_count + #{commentCount,jdbcType=INTEGER}
        where id = #{id}
    </update>
    <select id="selectRelated" parameterType="com.gs.community.model.Question" resultMap="BaseResultMap">
    select * from question where id !=#{id} and tag regexp #{tag}
    </select>
    <select id="countBySearch" parameterType="com.gs.community.dto.QuestionQueryDTO" resultType="java.lang.Integer">
        select count(*) from question
        <where>
            (status = 0 OR status = 1)
            <if test="search != null and search != ''">
                and title regexp #{search}
            </if>
            <if test="tag != null and tag != ''">
                and tag regexp #{tag}
            </if>
            <if test="sort != null and sort != '' and sort == 'no'">
                and comment_count = 0
            </if>
            <if test="sort != null and sort != '' and sort == 'good'">
                and status = 1
            </if>
            <if test="time != null and time != ''">
                and gmt_create > #{time}
            </if>
        </where>
    </select>
    <select id="selectBySearch" parameterType="com.gs.community.dto.QuestionQueryDTO" resultMap="BaseResultMap">
        select * from question
        <where>
            (status = 0 OR status = 1)
            <if test="search != null and search != ''">
                and title regexp #{search}
            </if>

            <if test="tag != null and tag != ''">
                and tag regexp #{tag}
            </if>
            <if test="sort != null and sort != '' and sort == 'no'">
                and comment_count = 0
            </if>
            <if test="sort != null and sort != '' and sort == 'good'">
                and status = 1
            </if>
            <if test="time != null and time != ''">
                and gmt_create > #{time}
            </if>
        </where>
        <if test="sort == null or sort == ''">
            order by gmt_latest_comment desc
        </if>
        <if test="sort != null and sort != '' and sort == 'new'">
            order by gmt_latest_comment desc
        </if>
        <if test="sort != null and sort != '' and sort == 'no'">
            order by gmt_latest_comment desc
        </if>
        <if test="sort != null and sort != '' and (sort == 'hot' || sort == 'hot7' || sort == 'hot30')">
            order by gmt_latest_comment desc
        </if>
        limit #{page},#{size}
    </select>

    <select id="selectTop" resultMap="BaseResultMap">
        select * from question
        <where>
            (status = 2 OR status = 3)
            <if test="search != null and search != ''">
                and title regexp #{search}
            </if>

            <if test="tag != null and tag != ''">
                and tag regexp #{tag}
            </if>
            <if test="sort != null and sort != '' and sort == 'no'">
                and comment_count = 0
            </if>
            <if test="sort != null and sort != '' and sort == 'good'">
                and status = 3
            </if>
            <if test="sort != null and sort != '' and sort == 'good'">
                order by gmt_latest_comment desc
            </if>
            <if test="time != null and time != ''">
                and gmt_create > #{time}
            </if>
        </where>
        <if test="sort == null or sort == ''">
            order by gmt_latest_comment desc
        </if>
        <if test="sort != null and sort != '' and sort == 'new'">
            order by gmt_latest_comment desc
        </if>
        <if test="sort != null and sort != '' and sort == 'no'">
            order by gmt_latest_comment desc
        </if>
        <if test="sort != null and sort != '' and (sort == 'hot' || sort == 'hot7' || sort == 'hot30')">
            order by gmt_latest_comment desc
        </if>
    </select>
</mapper>